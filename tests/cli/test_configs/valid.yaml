dim_config:
  owner:
    - email: dummy@mozmail.com
      slack_handle: dummy
  tier: tier_3
  dim_tests:
    - type: table_row_count
      options:
        condition: row_count <= 10
        alerts_enabled: disabled
