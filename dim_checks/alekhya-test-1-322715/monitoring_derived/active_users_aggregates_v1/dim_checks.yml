dim_config:
  - owner: akommasani@mozilla.com
    # tier: tier_1
    tests:
      - type: not_null
        config:
          columns:
            - segment 
          threshold: 10
      - type: uniqueness
        config:
            columns:
              - segment
              - app_version
            threshold: 10