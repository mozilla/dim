dim_config:
  - owner: akommasani@mozilla.com
    # tier: tier_1
    tests:
      - type: not_null
        config:
          columns:
            - segment 
          validation: row_count > 10
          enabled: True
          notifier: slack
          recipients: "#data-monitoring-mvp"
          alert-name: "test_alert_for_null"
      - type: uniqueness
        config:
            columns:
              - segment
              - app_version