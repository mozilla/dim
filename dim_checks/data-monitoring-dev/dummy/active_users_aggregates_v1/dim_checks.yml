dim_config:
  - owner: akommasani@mozilla.com
    # tier: tier_1
    tests:
      - type: not_null
        config:
          columns:
            - segment
      - type: uniqueness
        config:
            columns:
              - segment
              - app_version
