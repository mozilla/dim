dim_config:
  - owner: akommasani@mozilla.com
    # tier: tier_1
    tests:
      - type: not_null
        config:
          columns:
            - segment 
          validation: row_count > 10
      - type: uniqueness
        config:
            columns:
              - segment
              - app_version
